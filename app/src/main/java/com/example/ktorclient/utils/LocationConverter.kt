package com.example.ktorclient.utils

import androidx.room.TypeConverter
import com.example.ktorclient.response.allCharacterResponse.Location

class LocationConverter {
 /*   @TypeConverter
    fun fromLocation(location: Location?): String? {
        return location.toJson
    }

    @TypeConverter
    fun toLocation(json: String?): Location? {
        return json?.let { Location.fromJson(it) }
    }*/
}